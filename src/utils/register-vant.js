// 引入vue
import Vue from "vue";
// 引入vant
import {
    Button,
    Cell,
    NavBar,
    Form,
    Field,
    CountDown
} from "vant"
Vue.use(Button)
    .use(Cell)
    .use(NavBar)
    .use(Form)
    .use(Field)
    .use(CountDown)
